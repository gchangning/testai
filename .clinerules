# AI 核心行为准则

1. **绝对禁区**：严禁修改 `core/` 目录下的任何文件。
2. **业务隔离**：所有新业务代码必须写在 `modules/你的模块名` 下。
3. **调用原则**：如果业务需要底层功能，只能 `import` 调用，严禁修改实现。
4. **遇到框架缺陷**：如果必须修改 core 代码，请停止生成，并在代码注释中写明 `// TODO: 需架构师修改 core/xxx`。